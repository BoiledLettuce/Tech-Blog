<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TECH BLOG</title>
</head>

<div class="wrapper">
    <header>
        <h1>TECH BLOGS</h1>
        <nav class="navbar">
            {{#if loggedIn}}
            <a href="/">homepage</a>
            <a href="/dashboard">dashboard</a>
            <a href="/dashboard/create">create forum</a>
            <button id="logout" class="button">logout</button>
            {{else}}
            <a href="/">homepage</a>
            <a href="/login">login</a>
            {{/if}}
        </nav>
    </header>
    <main>
        {{{ body }}}
    </main>

    <footer>
        <div>
            <p>
                <a href="/">homepage</a>{{#if loggedIn}}
                <a href="/dashboard/">dashboard</a>
                <a href="/dashboard/create/">create new forum</a>
                {{else}}
                <a href="/login/">login</a>
                {{/if}}
            </p>
        </div>
        <div>
            <p>
                <strong>TECH BLOG</strong>
            </p>
        </div>
    </footer>
</div>

{{#if loggedIn}}
<script src="/public/js/logout.js"></script>
{{/if}}

    
</body>
</html>